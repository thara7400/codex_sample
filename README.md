# 会話デッキアプリ

このリポジトリには1on1用の会話テーマを管理するシンプルなWebアプリが含まれています。

## 使い方

1. `index.html` をブラウザで開きます。
2. テーマを追加するか、サンプルテーマを読み込みます。
3. "テーマ一覧" ボタンで一覧表示モード、"ランダム" ボタンでランダム表示モードに切り替えられます。
4. 追加したデータはブラウザの LocalStorage に保存されます。

Firebase Hosting など静的ホスティングにそのまま配置することで公開できます。
